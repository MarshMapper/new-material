{
    "$schema": "http://json-schema.org/schema",
    "$id": "projectSetup",
    "title": "Project Setup Options",
    "type": "object",
    "properties": {
      "defaultPageName": {
        "type": "string",
        "description": "The name of the default / primary page in the UI",
        "x-prompt": "What is the name of the default page in the UI?"
      }
    },
    "required": ["defaultPageName"]
  }